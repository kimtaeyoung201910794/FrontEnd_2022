console.log("7"+"4") ->74
자바스크립트는 타입변환을 할 수 없는 상황에서도 연산을 하기위해 타입 변환을 사용합니다.

자바스크립트는 데이터 타입을 자기가 맞다고 생각하는 대로 변환함
그래서 우리는 보다 정확하게 데이터 타입을 넘겨줘야 한다.


{trusly falsy}-> true같은거, false같은거.
실제로 그 값들이 true나 false는 아니지만 그런거 같은거 ..

prompt(); -> 입력창 띄워줌 입력창에 사용자가 입력한 값을 반환해줌.

!undefined
!null
!o
!''
!NaN 
->TRUE 

<함수>
입출력이 선택적
1.functuon add(a,b){
    return a+b;
} 입력은 여러개지만 출력은 하나여야 한다 

2. functuon print(message){
    console.log(message) 
    ->return이 없음 
}

3.function a{
    return a
} ->출력만있음 